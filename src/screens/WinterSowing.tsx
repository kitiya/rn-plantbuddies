import React from "react";
import { View, Text, StyleSheet } from "react-native";

const WinterSowing = () => {
  return (
    <View style={styles.screen}>
      <Text>Winter Sowing Screen</Text>
    </View>
  );
};

export default WinterSowing;

const styles = StyleSheet.create({
  screen: {
    flex: 1,
  },
});
